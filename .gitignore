__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ROS2 / colcon generated output
build/
install/
log/

# Python environments and local IDE state
.venv/
venv/
env/
.idea/
.vscode/

# Runtime captures that should be copied intentionally if needed
*.bag
*.db3
*.mp4
*.avi
*.mkv

# Robot runtime outputs are preserved in local factory-reset backups, not added by default.
g1_hand_arm_project/logs/
g1_hand_arm_project/vision/debug/

# Local GUI terminal is not part of the public showcase repository.
hybrid_control_terminal/python_gui/

# Large exported inference artifacts. Small .pt files in models/ are intentional.
*.engine
*.onnx
!models/*.pt

# Local-only factory-reset backup folder lives outside the repo.
factory_reset_backup_*/
